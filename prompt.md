nemo-stock (네모네모매매) 프로젝트를 개발합니다. 기획서는 nemo-stock.md를 참조하십시오. 이 기획의 PoC를 제작할 것입니다.

가장 중요한, AI를 사용해서 로직을 구현하고, 실행하고, 백테스트하는 기능을 PoC로써 구현할 것입니다. 구현의 상세 방법을 정의합니다.

백엔드: Python FastAPI를 사용합니다. 각 예약된 트리거 노드 실행의 경우 쓰레드나 Process Pool을 만들고, 트리거가 저장되는 트리거 Queue를 만들어서 풀링해서 하나씩 노드를 실행합니다. (필요한 경우 백엔드 프로세스가 아닌 별도의 Batch 전용 프로세스를 사용할 수 있습니다.) 이 부분은 인메모리 DB나 분산 운영에 장점이 있는 Kafka 등으로 대체하는 것이 나은 경우, 나중에 교체하기 쉽도록 queue를 인터페이스 형태로 구현해야 합니다. 각 노드의 실행은 프로세스나 스레드 안에서 연결 관계를 분석하여 queue에 넣고 (위상 정렬), 차례로 실행되도록 해야 합니다. 주식 매매나 호가 정보는 테스트용으로 불러올 수 있되, 실제 서비스와 연계도 가능하도록 모듈 추가나 더미 모듈 교체를 염두한 설계가 되어야 합니다. Toss 증권의 API 문서를 찾아 참고하십시오.
프론트엔드: html이나 vue 등 적절한 웹 기반 프레임워크 사용. 블럭 코딩처럼 쉽게 수정 가능해야하고, 백 테스트나 테스트를 수행하는 경우, 수행되는 블럭이 깜빡이며 디버그 값을 볼 수 있어야 합니다. 테스트 하는 경우에는 임의의 값들을 사용해서 테스트할 수 있고, 해당 값을 백엔드에서 가져오는 방법을 정의해야 합니다. 웹 공공 데이터로부터 데이터를 받아 와서 해당 테스트를 진행할 수 있는 값들을 sqlite에 저장하십시오. 
AI: OpenAI API를 사용합니다. 키는 별도로 입력할 수 있어야 합니다. 키가 프론트엔드에 공개되어선 안 되므로, 백엔드의 환경 설정에서 세팅할 수 있어야 합니다. Dart(공시)의 각 내용을 저장하는 경우, AI를 사용해서 점수 등을 계산할 수 있어야 합니다. 이 값은 캐싱되어 AI의 사용을 최소한으로 하도록 하여야 합니다.
인프라: 현재 PoC에서는 Python FastAPI를 사용하고, RDB가 필요한 경우 sqlite를 사용합니다. dao를 사용해서 추후 다른 RDB로 교체하기 쉽게 설계되어야 합니다. 인메모리 DB도 사용할 수 있어야 합니다. Redis나 Kafka 사용을 할 수 있지만, 현재 PoC 구현은 Python의 객체로 구현하고, 마찬가지로 추후 모듈 변경만으로 완벽히 대체할 수 있도록 설계합니다.
노드: 각 노드는 Python의 ABC 클래스를 사용해서 수정 가능하게 구현합니다. 파이프라인을 인식하고 인과관계대로 실행될 수 있도록 구현하십시오.


코드를 작성하기 전에 반드시 설계하고, 설계도 내용을 담은 md 파일을 생성하십시오. 해당 설계도를 바탕으로 개발하고, 테스트 케이스를 만들어 테스트하십시오. 확정되지 않은 내용이나 더 필요한 정보가 있으면 반드시 일시 정지하고 반문하십시오. 설계도 내용이 작성되고 개발이 시작되면 테스트가 가능할 때까지 반문하지 말고 개발을 계속하시오.